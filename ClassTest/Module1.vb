Module Module1
    Class Duck
        Public Property Name As String
        Public Property Color As String
        Public Property Age As Integer
        Public Property Breed As String
        Sub New()

        End Sub

        Sub New(name As String, color As String, age As Integer, breed As String)
            Me.Name = name
            Me.Color = color
            Me.Age = age
            Me.Breed = breed

        End Sub


        Public Sub Swin()

        End Sub

    End Class
End Module
